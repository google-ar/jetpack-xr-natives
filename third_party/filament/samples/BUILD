licenses(["notice"])

package(
    # Samples should not be depended upon.
    default_visibility = ["//visibility:public"],
)

# These Filament samples are not trivial to build in google3, and thus have no
# extant build rules. They are mainly synced as code references to be indexed by
# CodeSearch for Filament app developers.
